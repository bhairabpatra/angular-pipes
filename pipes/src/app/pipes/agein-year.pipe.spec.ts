import { AgeinYearPipe } from './agein-year.pipe';

describe('AgeinYearPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeinYearPipe();
    expect(pipe).toBeTruthy();
  });
});
